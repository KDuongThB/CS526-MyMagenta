const {stepTypeController} = require("../controllers/stepTypeController");

const router = require("express").Router();

// Your code right here
// router.post("/", stepTypeController.addNewStepType)
module.exports = router;